rfkill list

sudo rfkill unblock all


sudo apt install network-manager

nmcli radio

nmcli radio wifi on

nmcli con reload

nmtui

apt-get update -y && apt update -y && apt-get upgrade -y && apt upgrade -y && apt install aptitude -y && aptitude full-upgrade -y

https://wireless.docs.kernel.org/en/latest/en/users/drivers/b43.html


sudo apt-get install dkms build-essential linux-headers-$(uname -r)
sudo apt-get install build-essential dkms linux-headers-$(uname -r)


sudo apt-get install bcmwl-kernel-source



If Wi-Fi still doesn’t work:
Run lspci -nn | grep Network to confirm your Broadcom chipset.
Run lsmod | grep wl to verify if the wl driver is loaded.
If it conflicts with b43 or ssb, blacklist those modules:
sudo modprobe -r b43 ssb
sudo modprobe wl


lsmod | grep wl
sudo dkms status

sudo rm -rf /tmp/*


echo "blacklist b43" | sudo tee -a /etc/modprobe.d/blacklist.conf
echo "blacklist ssb" | sudo tee -a /etc/modprobe.d/blacklist.conf

lspci -nn | grep Network



sudo apt install broadcom-sta-dkms


sudo modprobe -r iwlwifi

sudo systemctl restart NetworkManager 

sudo modprobe wl 





sudo rm /var/crash/broadcom-sta-dkms.0.crash


sudo dkms remove -m broadcom-sta -v 6.30.223.271 --all
sudo dkms add -m broadcom-sta -v 6.30.223.271
sudo dkms build -m broadcom-sta -v 6.30.223.271
sudo dkms install -m broadcom-sta -v 6.30.223.271

sudo apt-get remove --purge broadcom-sta-dkms
sudo apt-get install broadcom-sta-dkms



sudo depmod -a
sudo update-initramfs -u

apt install bcwl-kernel-source

